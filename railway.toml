[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn backend.pg_server:app --host 0.0.0.0 --port $PORT --workers 1"
healthcheckPath = "/health/live"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
